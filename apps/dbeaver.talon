app.exe: dbeaver
-


pin that:
	key(ctrl-shift-p)

run that:
	key(ctrl-enter)

file create:
	key(ctrl-])

format that:
	key(ctrl-shift-f)

zap:
	key(esc esc esc)

with:
    "with base as (select * from source) select * from base"
    key(ctrl-shift-f)


block comment:
	"/*"
	key(enter)
